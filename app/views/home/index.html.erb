<div class="container">
  <div class="urls">
    <% @articles.map do |_, article| %>
      <%if not article.empty? %>
        <%= render partial: "list", locals: { target: article.first.target, articles: article } %>
      <% end %>
    <% end %>
  </div>
</div>